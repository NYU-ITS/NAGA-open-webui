// Feature flags for local / frontend-only development.
// Set frontendOnlyMode = true to bypass the "Backend Required" error
// and run the UI without a backend server.
export const frontendOnlyMode = false;
